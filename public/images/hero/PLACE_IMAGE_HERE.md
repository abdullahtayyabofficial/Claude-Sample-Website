# Hero Image

Save your professional conference-room photo here as:

  `public/images/hero/abdullah-tayyab.jpg`

Recommended: the seated boardroom photo (landscape crop, high resolution).
The code uses `object-top` so the face + upper body is always visible regardless of crop.
