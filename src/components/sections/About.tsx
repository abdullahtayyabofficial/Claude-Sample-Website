'use client'

// About section — implemented in Step 3 (awaiting CV / portfolio data)
export default function About() {
  return (
    <section id="about" className="section-padding bg-[var(--color-surface-muted)]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <p className="text-[var(--color-text-muted)] text-sm">[About Section — Awaiting CV Data]</p>
      </div>
    </section>
  )
}
