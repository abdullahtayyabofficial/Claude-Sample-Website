'use client'

// Testimonials carousel — implemented in Step 3 (awaiting testimonial screenshots)
export default function Testimonials() {
  return (
    <section id="testimonials" className="section-padding">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <p className="text-[var(--color-text-muted)] text-sm">[Testimonials — Awaiting Screenshots]</p>
      </div>
    </section>
  )
}
