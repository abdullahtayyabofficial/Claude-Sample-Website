'use client'

// Client logo proof strip — implemented in Step 3 (awaiting logo assets)
export default function ProofStrip() {
  return (
    <section className="py-12 border-y border-[var(--color-border)]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <p className="text-[var(--color-text-muted)] text-sm">[Client Logo Strip — Awaiting Assets]</p>
      </div>
    </section>
  )
}
